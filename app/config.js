// Shared API base URLs — change these when switching between local and cloud
export const API_URL = 'http://localhost:8001';  // core server (auth, chat)
export const AI_URL  = 'http://localhost:8000';  // AI server (enroll, process)
